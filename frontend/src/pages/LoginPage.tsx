import { Anchor, Button, Container, Paper, PasswordInput, Stack, TextInput, Title } from '@mantine/core';
import { FormEvent, useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../auth/AuthContext';
import { ErrorList } from '../components/ErrorList';
export function LoginPage(){ const {signIn}=useAuth(); const nav=useNavigate(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [errors,setErrors]=useState<any>(); async function submit(e:FormEvent){ e.preventDefault(); try{ await signIn(email,password); nav('/'); }catch(err){ setErrors(err); } } return <Container size="xs"><Paper withBorder shadow="sm" radius="md" p="xl"><Stack><Title order={1}>Sign in</Title><Anchor component={Link} to="/register">Need an account?</Anchor><ErrorList errors={errors}/><form onSubmit={submit}><Stack><TextInput label="Email" aria-label="Email" value={email} onChange={e=>setEmail(e.currentTarget.value)}/><PasswordInput label="Password" aria-label="Password" value={password} onChange={e=>setPassword(e.currentTarget.value)}/><Button type="submit" color="green">Sign in</Button></Stack></form></Stack></Paper></Container>; }
