import { Anchor, Button, Container, Paper, PasswordInput, Stack, TextInput, Title } from '@mantine/core';
import { FormEvent, useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { useAuth } from '../auth/AuthContext';
import { ErrorList } from '../components/ErrorList';
export function RegisterPage(){ const {signUp}=useAuth(); const nav=useNavigate(); const [username,setUsername]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [errors,setErrors]=useState<any>(); async function submit(e:FormEvent){ e.preventDefault(); try{ await signUp(username,email,password); nav('/'); }catch(err){ setErrors(err); } } return <Container size="xs"><Paper withBorder shadow="sm" radius="md" p="xl"><Stack><Title order={1}>Sign up</Title><Anchor component={Link} to="/login">Have an account?</Anchor><ErrorList errors={errors}/><form onSubmit={submit}><Stack><TextInput label="Username" aria-label="Username" value={username} onChange={e=>setUsername(e.currentTarget.value)}/><TextInput label="Email" aria-label="Email" value={email} onChange={e=>setEmail(e.currentTarget.value)}/><PasswordInput label="Password" aria-label="Password" value={password} onChange={e=>setPassword(e.currentTarget.value)}/><Button type="submit" color="green">Sign up</Button></Stack></form></Stack></Paper></Container>; }
